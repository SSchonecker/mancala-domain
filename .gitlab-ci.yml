stages:
  - test
  - visualize

maven_test:
  stage: test
  image: maven:latest
  tags:
    - maven
  script:
    - Write-Host "Building project with maven"
    - mvn verify

    - mvn $MAVEN_CLI_OPTS clean org.jacoco:jacoco-maven-plugin:prepare-agent test jacoco:report
  artifacts:
    when: always
    reports:
      junit:
        - target/surefire-reports/TEST-*.xml
        #- target/failsafe-reports/TEST-*.xml
    paths: 
      - target/surefire-reports/TEST-nl.sogyo.mancala.domain.PitTest.xml
      - target/site/jacoco/jacoco.xml

coverage-convert:
  stage: visualize
  image: haynes/jacoco2cobertura:1.0.4
  script:
    - ls
    # convert report from jacoco to cobertura
    - py /opt/cover2cover.py target/site/jacoco/jacoco.xml src/main/java > target/site/cobertura.xml
    # read the <source></source> tag and prepend the path to every filename attribute
    - py /opt/source2filename.py target/site/cobertura.xml
  needs: ["maven_test"]
  dependencies:
    - maven_test
  artifacts:
    reports:
      cobertura: target/site/cobertura.xml
